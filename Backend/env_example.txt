# Flask Configuration
SECRET_KEY=your-secret-key-here
PORT=5000
DEBUG=True

# Render Cron Job Configuration
# ANALYSIS_PAYLOAD should contain the full one-line JSON payload used by Backend/run_background_analysis.py.
ANALYSIS_PAYLOAD={"case_id":"example_case_id","keywords":["example"],"country":"United States","claims":["example claim"],"owners":["example owner"],"context":"example context","search_limitations":{"companies":["example owner"],"urls":[]}}

# OpenAI API Configuration (for text embeddings)
# OPENAI_API_KEY=sk-your-key-here

# Firebase Configuration (for authentication and cloud services)
# FIREBASE_CREDENTIALS_PATH=path/to/firebase-service-account.json
# FIREBASE_PROJECT_ID=your-firebase-project-id

# CORS Configuration
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
